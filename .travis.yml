language: rust
rust:
- nightly

script: cd $CRATE && cargo build --verbose && cargo test --verbose

env:
- CRATE=factorio-mods-common
- CRATE=factorio-mods-local
- CRATE=factorio-mods-web
- CRATE=.

sudo: false
